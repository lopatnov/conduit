{
  "name": "conduit-demo",
  "private": true,
  "version": "0.0.1",
  "description": "Conduit demo — integration tests via Playwright",
  "type": "module",
  "scripts": {
    "start:api": "node api/server.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
