# Session rotation log

> **Policy retired 2026-08-29** (`/retro`, user decision) — periodic full rotation
> (`session-rotate.md`, deleted) didn't save anything for this routine's daily cadence
> (cache TTL is 1h, firings are 24h apart) and introduced a real tool-access bug on top. See
> `.claude/rules/index.md` "Session rotation retired" and `feature-workspace-cycle.md`
> Step 0a. This file's history below is kept for the record — no new rows are expected under
> the old policy, though the *manual* case (`/handoff`) can still reference it for context.

> Append-only. Заведено 2026-08-28 по прямому запросу пользователя после того, как эта же
> непрерывная self-bind сессия (живая с 2026-07-29) реально упёрлась в session-wide rate limit
> модели прямо посреди разбора security-алертов — из-за накопленного за месяц контекста без единой
> ротации. Механизм (см. `.claude/commands/session-rotate.md` для полной процедуры, включая
> **исправление того же дня**: `create_session` для новой сессии больше НЕ используется — GitHub
> MCP-тулы недоступны в сессии, созданной так, только пользователем напрямую):
> `.claude/commands/feature-workspace-cycle.md` Step 0a — каждое срабатывание считает дневные
> лог-записи с последней ротации (или с начала, если таблица ниже пуста), и при ~15 срабатываниях
> (середина целевого диапазона 10-20) старая сессия просит пользователя создать новую сессию
> самостоятельно, перевешивает Routine на неё (`create_trigger` с `persistent_session_id`) и
> удаляет свою старую привязку (`delete_trigger`) — после чего перестаёт получать срабатывания.
> Новая строка добавляется здесь при каждой такой ротации; новая сессия при первом же срабатывании
> должна прочитать этот файл целиком (см. Step 0 самого `feature-workspace-cycle.md`), включая
> собственно эту таблицу, чтобы знать когда была прошлая ротация.

| Date | Old session | New session | ~Firings since last rotation | Reason |
|---|---|---|---|---|
| 2026-08-28 ~21:52 UTC | `session_01J7m8YnRBV2er7F3jYMexMg` (alive since 2026-07-29, this is its first-ever rotation) | `session_01DmUkKXPvj2xAEvRdCTux3G` | ~50 (rough count of dated "Реализовано в сессии" headings + hygiene-log rows across this file's entire history — no prior rotation to count *since*, so this is effectively "since the routine's creation") | User-requested first rotation, prompted directly by hitting the account's session-wide model rate limit mid-firing (~16:2x UTC same day, while triaging a pasted GitHub Security-tab screenshot batch via `security-engineer` — the delegation itself failed with `rate_limit`/HTTP 429). Routine `trig_01K6vntPGmn2hVGAXw5Ew6Ra` deleted; new trigger `trig_01HGENoJ5nioWvWzbtCBL9Js` created with identical `cron_expression` (`0 1 * * *`) and `prompt` (`/feature-workspace-cycle`), pointed at the new session via `persistent_session_id`. **Unfinished handoff item**: the security-tab triage (23 open CodeQL alerts on `main` + several Dependabot/OSV CVEs, screenshots pasted by the user, not yet in this log) was interrupted mid-investigation by the rate limit and never completed — the new session should check with the user whether to resume it, details are in this conversation's own transcript, not yet distilled into a CLAUDE.md entry at rotation time.
| 2026-08-28 ~22:00 UTC (same-day addendum, new session's own first turn) | `session_01J7m8YnRBV2er7F3jYMexMg` | `session_01DmUkKXPvj2xAEvRdCTux3G` | (continuation of the row above, not a second rotation) | **Bigger finding, confirmed by the user directly to the new session**: `session_01DmUkKXPvj2xAEvRdCTux3G` came up with **zero GitHub API/MCP tool access** — no `gh` CLI, no `mcp__github__*` tools (`ToolSearch` finds none), no GitHub entry in `ListConnectors` (only Gmail/Calendar/Drive listed). Only unauthenticated `WebFetch` on public pages and plain `git clone`/`push` work. The user says this is a known, repeatable pattern: a session spawned via `mcp__Claude_Code_Remote__create_session` (one Claude session creating another) never gets GitHub tool access; a session the *user* creates directly always does. Root cause unknown — the user's words: "это твой баг" (a platform bug, not something this repo can fix). Since `feature-workspace-cycle.md` Steps 1/2/6-9 all depend on GitHub MCP tools, this replacement session **cannot actually run the cycle** — orient/report only. `.claude/commands/session-rotate.md` updated same day to stop using `create_session` for future rotations and instead have the old session ask the user to create the replacement directly. As of this row, the Routine (`trig_01HGENoJ5nioWvWzbtCBL9Js`) still points at this GitHub-tool-less session pending the user creating a proper replacement — next daily firing (2026-08-29 01:00 UTC) will stall at Step 1 unless that happens first. |
| 2026-08-28 ~22:10 UTC (handoff completion, prompted by the user directly) | `session_01DmUkKXPvj2xAEvRdCTux3G` (GitHub-tool-less, never ran the cycle) | `session_01WhHVM9QyJDcadMX6fQtdXd` | (continuation, not a new rotation — this is the user creating the replacement the previous row asked for) | User created this session directly (via the desktop app, not `create_session`) specifically to become the cycle's new home, confirming the pattern from the row above: `mcp__github__get_me` succeeds immediately here, `ListConnectors`/repo-scope tools all present. This session repointed the Routine itself — created `trig_01Ehd6ceyaWxB6aytQwuydsp` (identical `cron_expression` `0 1 * * *` and `prompt` `/feature-workspace-cycle`) with `persistent_session_id` set to itself, then deleted the stranded `trig_01HGENoJ5nioWvWzbtCBL9Js`. **One caveat surfaced by `create_trigger`'s own response**: it warned "this trigger stores no MCP connectors, so the sessions it fires will run without connector tools" — worth double-checking at the very next firing (2026-08-29 01:0x UTC) that `mcp__github__*` tools are still present, since the warning's wording doesn't distinguish self-bind/persistent-session firings (which just resume this already-configured session — expected fine) from the fresh-session case the warning seems aimed at. If the next firing *does* come up without GitHub tools, that would mean even a same-session Routine firing can drop them, which is a materially different (and worse) finding than anything logged in the rows above — flag it loudly if so. No code/process change was needed beyond what `session-rotate.md` already had (see `c6804b7`) — this row is purely confirming the fix works end-to-end. |
