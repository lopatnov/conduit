# OSV-Scanner ignore list — https://google.github.io/osv-scanner/configuration/
# Each entry is a documented risk-acceptance, not a permanent suppression.
# Re-evaluate when the conditions in "reason" change.
#
# No vulnerabilities are ignored at the moment.
#
# Real CVEs (rsa@0.9.10 / CVE-2023-49092) are intentionally NOT listed here —
# left open in the Security tab until an upstream fix is available. See
# .claude/logs/session-log.md (2026-06-12 entry) for the risk analysis.
#
# History: RUSTSEC-2025-0069 (daemonize unmaintained) was ignored while
# pingora-core 0.8.1 depended on it; Pingora 0.9 replaced it with daemonix, so
# the entry was removed. protobuf@2.28.0 / CVE-2025-53605 left the dependency
# tree in the same upgrade (pingora-core stopped depending on prometheus 0.13).
